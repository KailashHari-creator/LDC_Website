import IPC from './ipc';
import { Network } from './network';
var channel = new Network(IPC);
var ExpressSocket = /** @class */ (function () {
    function ExpressSocket() {
    }
    ExpressSocket.prototype.send = function (event, callback) {
        channel.send(event.name, event, callback);
    };
    ExpressSocket.prototype.queue = function (event, callback) {
        channel.receive(event.name, callback);
        if (!callback) {
            channel.receive(event.name, callback);
        }
    };
    ExpressSocket.prototype.connect = function (host, callback) {
        channel.connect(host, callback);
    };
    return ExpressSocket;
}());
export { ExpressSocket };
//# sourceMappingURL=index.js.map